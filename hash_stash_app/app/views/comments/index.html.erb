<h1>Hello from all the comments</h1>
<br>
  <a href='/comments/new'>Add New Comment</a>
<br>
<ul>
<% @comments.each do |comment| %>
  <li>
  <%= link_to(comment.content, "/comments/#{comment.id}") %>
  </li>
<% end %>
</ul>